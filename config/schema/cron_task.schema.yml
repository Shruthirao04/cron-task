# Schema for the configuration files of the cron task module.
cron_task.settings:
  type: config_object
  label: 'cron task settings'
  mapping:
    example:
      type: string
      label: 'Example'
